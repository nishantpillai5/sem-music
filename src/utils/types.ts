export type SongType = {
  title: string;
};

export type AlbumType = {
  title: string;
};

export type ArtistType = {
  title: string;
};

export type FetchType = {
  fetch: {
    termType: string;
    value: string;
  };
};
